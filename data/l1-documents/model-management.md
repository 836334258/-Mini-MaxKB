# 模型管理

Mini-MaxKB 把模型供应商和模型能力分开管理。对话应用只依赖统一的聊天接口，因此可以在 Gemini 和 DeepSeek 之间切换，而不用修改知识库代码。

聊天模型与向量模型是两个独立配置。聊天模型负责生成回答，Embedding 模型负责把文档和问题转换成向量。

更换聊天模型不会影响已经建立的文档索引。更换 Embedding 模型或向量维度后，旧向量不能继续比较，必须重新向量化全部文档并重建索引。
